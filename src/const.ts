export const TOKEN_REFRESH_THRESHOLD = 60; // Threshold value for token refresh (in seconds)
export const TOKEN_EXPRES_IN = 3600;
export enum AppRoute {
  Main = "/",
  AboutHealth = "/health",
  AboutRepose = "/repose"
}
